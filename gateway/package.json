{
  "name": "@skillsync/gateway",
  "version": "1.0.0",
  "private": true,
  "description": "API gateway for SkillSync (JWT, blacklist, CSRF, rate limit, CORS, proxy)",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "dev": "node src/server.js",
    "start": "node src/server.js"
  }
}
